
import 'package:flutter/material.dart';

class AppColor {
  static const Color primaryText = Color.fromARGB(255, 45, 45, 47);
  static const Color primaryElement = Color.fromARGB(255, 30, 152, 255);
  static const Color primaryElementText = Color.fromARGB(255, 255, 255, 255);
  // ColorScheme colorScheme = ColorScheme.fromSeed(seedColor: Colors.blueAccent);
  // static double appBarHeight = AppBar().preferredSize.height;

}